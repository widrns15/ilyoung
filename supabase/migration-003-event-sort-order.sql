-- ============================================================
-- 마이그레이션 003: 일정 수동 정렬 순서
-- 같은 날 일정을 꾹 눌러 위아래로 재정렬할 수 있도록 sort_order 추가.
-- schema.sql 을 이미 적용한 기존 DB에는 이 파일만 Run 하세요.
-- ============================================================

alter table public.events
  add column sort_order integer not null default 0;
